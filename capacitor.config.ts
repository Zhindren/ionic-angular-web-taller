import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.angular.taller.unidad.i',
  appName: 'ionic-angular-taller-unidad-i',
  webDir: 'www'
};

export default config;
